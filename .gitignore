# fish
/config/fish/*
!/config/fish/config.fish
!/config/fish/fish_plugins

# Neovim
/config/nvim/lazy-lock.json

# Zsh
# ZDOTDIR がこのディレクトリなので、`compinit` を -d なしで実行する
# 外部ツール (子 zsh を起動するもの) が .zcompdump を落としていくことがある
/config/zsh/.zcompdump*
